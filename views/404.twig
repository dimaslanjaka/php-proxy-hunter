<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error 404 - Not Found</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 flex items-center justify-center min-h-screen">
<div class="bg-white max-w-lg mx-auto border border-gray-300 rounded-lg shadow-md p-6">
  <h1 class="text-2xl font-bold text-red-600">Error 404 - Not Found</h1>
  <p class="mt-4">{{ errorMessage }}</p>
  {% if debug %}
    <div class="mt-6 text-sm text-gray-600">
      <p><strong>File:</strong> {{ errorFile }}</p>
      <p><strong>Line:</strong> {{ errorLine }}</p>
    </div>
  {% endif %}
</div>
</body>
</html>
