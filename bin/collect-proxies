#!/usr/bin/env bash
# Wrapper to run artisan/proxyCollector.py from project root, forwarding all args
CWD="$(cd "$(dirname "$0")/.." >/dev/null 2>&1 && pwd)"

# Always use project-local bin/py
exec "$CWD/bin/py" "$CWD/artisan/proxyCollector.py" "$@"
