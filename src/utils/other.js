/**
 * A no-operation function that does nothing.
 * Commonly used as a placeholder or default callback.
 *
 * @param {...any} _args - Any arguments passed to the function.
 */
export function noop(..._args) {
  // No operation performed
}
